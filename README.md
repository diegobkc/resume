# Brian Jones — Resume

Live at https://diegobkc.github.io/resume/

Source of truth for this site is the private working copy at
`../resume/brian-jones-resume.html` (includes phone number, used for the PDF
sent directly to recruiters). This repo's `index.html` is a public-safe copy
with the phone number removed, since GitHub Pages content is public and
crawlable.

To update: edit the private copy, regenerate this repo's `index.html` (strip
the phone span + QR block) and `resume.pdf`, then commit and push.
